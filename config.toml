[tracker]
# poll_interval_hours is hardcoded to 6 in scheduler.py
# Routes are managed from the frontend UI — no config needed here.

# Email credentials go in .env (see .env.example), not here.
# SMTP host/port default to smtp.gmail.com:587 and can be overridden via
# SMTP_HOST and SMTP_PORT env vars if needed.
